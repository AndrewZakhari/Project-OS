import java.util.List;

public class PriorityScheduler {

    public List<Process> executePriority(List<Process> processes) {
        // TODO (Yassin): Initialize time tracking and queue variables.

        // Inside the time loop:
            // TODO (Yassin): Implement Priority selection logic. 
            // Scan the queue for the highest priority process (lowest integer).

            // TODO (Marina): Implement tie-breaking rules.
            // What happens if two processes have the same priority at the exact same time?
            // Fall back to FCFS based on arrival time.

            // TODO (Yassin): Calculate WT, TAT, and RT for the selected process once it finishes.

        // return the chronological execution order
        return null; // Placeholder
    }
}
